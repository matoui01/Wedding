# No guest data in this repository. It is public.
#
# Both files carry real people: names, which side they belong to, which
# category and priority they were put in — and guests-live.csv additionally
# carries every household's private token, which is the key to their RSVP.
# Regenerate the seed with migrate-xlsx.py; re-export the live one from the
# sheet when it is needed.
guests-seed.csv
guests-live.csv
