# fetched on first run by render-card.js
.fonts/
node_modules/
# one invitation card per guest, each carrying a name and a personal note.
# They go into the couple's private Drive folder, never into this public repo.
cards-out/
